---
import { ArrowRight } from "@lucide/astro";
---

<!-- Hero Section - Performance Enhancement for K8 Nutrition -->
<section
  class="bg-gradient-to-br from-blue-600 to-blue-700 text-white py-16 sm:py-20 md:py-24 px-4 sm:px-6 text-center"
>
  <div class="max-w-4xl mx-auto">
    <!-- Main Heading with highlighted keyword -->
    <h1
      class="text-4xl sm:text-5xl md:text-6xl font-bold mb-4 sm:mb-6 font-sans leading-tight"
    >
      Aumenta tu <span class="text-blue-100">rendimiento</span>
    </h1>

    <!-- Subheading -->
    <p
      class="text-lg sm:text-xl md:text-2xl text-blue-100 mb-6 sm:mb-8 font-sans max-w-2xl mx-auto"
    >
      Suplementos de calidad al precio que todos merecen
    </p>

    <!-- Call to Action Button -->
    <a
      href="/catalog"
      class="inline-flex items-center bg-white text-blue-600 font-semibold py-3 px-6 rounded-lg text-base sm:text-lg transition-all duration-300 hover:bg-blue-50 hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-blue-600"
    >
      Ver productos
      <ArrowRight class="ml-2 h-5 w-5" />
    </a>
  </div>
</section>
